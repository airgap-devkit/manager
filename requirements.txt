# Deprecated — see pyproject.toml for canonical dependencies.
# Kept here for reference and for tooling that reads requirements.txt directly.
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
jinja2>=3.1.0
aiofiles>=23.0.0
